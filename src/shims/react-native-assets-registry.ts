export function getAssetByID(asset: any) {
  return asset;
}

export function registerAsset(asset: any) {
  return asset;
}

const AssetRegistry = {
  getAssetByID,
  registerAsset,
};

export default AssetRegistry;